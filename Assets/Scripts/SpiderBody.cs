using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpiderBody : MonoBehaviour
{
    [SerializeField] SpiderFoot frontLeft;
    [SerializeField] SpiderFoot frontRight;
    [SerializeField] SpiderFoot backLeft;
    [SerializeField] SpiderFoot backtRight;

    bool moveCondition = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
